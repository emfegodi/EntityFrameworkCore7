@model IEnumerable<CursoEntityCore.Models.Articulo>
@{
    ViewData["Title"] = "Lista de articulos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-4">
        <h3>Lista de artículos</h3>
    </div>
    <div class="col-3">
        <a asp-action="Crear" class="btn btn-primary form-control">Crear artículo</a>
    </div>
    @*<div class="col-5">
        <a asp-action="CrearMultipleOpcion2" class="btn btn-secondary form-control">Crear Multiple (2 artículos)</a>
        <br />
        <br />
        <a asp-action="CrearMultipleOpcion5" class="btn btn-secondary form-control">Crear Multiple (5 artículos)</a>
        <br />
        <br />
        <a asp-action="VistaCrearMultipleOpcionFormulario" class="btn btn-secondary form-control">Crear Multiples artículos en formulario</a>
        <br />
        <br />
        <a asp-action="BorrarMultiple2" class="btn btn-warning form-control">BorrarMultiple2</a>
        <br />
        <br />
        <a asp-action="BorrarMultiple5" class="btn btn-warning form-control">BorrarMultiple5</a>

    </div>*@

</div>

<div>
    @if (Model.Count() > 0)
    {
        <table class="table table-striped border">
            <tr>
                <th>@Html.DisplayNameFor(a => a.Articulo_Id)</th>
                <th>@Html.DisplayNameFor(a => a.Descripcion)</th>
                <th>@Html.DisplayNameFor(a => a.Fecha)</th>
                <th>@Html.DisplayNameFor(a => a.Calificacion)</th>
                <th>@Html.DisplayNameFor(a => a.Categoria)</th>
            </tr>
            @foreach (var articulo in Model)
            {
                <tr>
                    <td>@articulo.TituloArticulo</td>
                    <td>@articulo.Descripcion</td>
                    <td>@articulo.Fecha</td>
                    <td>@articulo.Calificacion</td>
                    <td>@articulo.Categoria_Id</td>
                    <td>
                        <a class="btn btn-primary text-white" asp-action="Editar" asp-route-id="@articulo.Articulo_Id">Editar</a>
                        <a class="btn btn-danger text-white" asp-action="Borrar" asp-route-id="@articulo.Articulo_Id">Borrar</a>
                    </td>
                </tr>
            }
        </table>
    }
    else
    {
        <p>No hay artículos</p>
    }
</div>